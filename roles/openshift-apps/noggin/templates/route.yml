apiVersion: v1
kind: Route
metadata:
  name: noggin-web
  labels:
    app: noggin
spec:
  host: account{{ env_suffix }}.fedoraproject.org
  port:
    targetPort: web
  to:
    kind: Service
    name: noggin-web
  tls:
    termination: edge
    insecureEdgeTerminationPolicy: Redirect
