apiVersion: v1
kind: Service
metadata:
  name: noggin-web
  namespace: aaa
  labels:
    app: noggin
spec:
  ports:
    - name: noggin-web
      port: 8080
      targetPort: 8080
  selector:
    app: noggin
    deploymentconfig: noggin
