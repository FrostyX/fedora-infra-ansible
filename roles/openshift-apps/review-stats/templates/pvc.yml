apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: openshift-{{env_short}}-reviewstats
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 1Gi
  storageClassName: ""
