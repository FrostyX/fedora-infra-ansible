apiVersion: v1
kind: Service
metadata:
  labels:
    app: testdays
  name: testdays
spec:
  ports:
  - name: 80-tcp
    port: 80
    protocol: TCP
    targetPort: 80
  selector:
    app: testdays
    deploymentconfig: testdays
