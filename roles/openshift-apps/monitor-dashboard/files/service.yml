apiVersion: v1
kind: Service
metadata:
  name: monitor-dashboard-web
  labels:
    app: dashboard
spec:
  selector:
    app: dashboard
    service: web
  ports:
  - name: web
    port: 8080
    targetPort: 3000
